RSpec.describe Store::Digest::HTTP do
  it "has a version number" do
    expect(Store::Digest::HTTP::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
